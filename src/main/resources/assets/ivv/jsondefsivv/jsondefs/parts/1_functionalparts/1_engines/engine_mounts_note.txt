Electric motors will remain as `engine_car` type until I see a majority of vehicles in mts able to take an `engine_car_electric` motor.
For now, they remain as "car engines" for the foreseeable futures.
